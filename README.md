# Img

个人素材与数据文件库：存放项目用到的图片、动效与 JSON 数据文件。

## 内容

- **图片素材**：照片、头像、二维码、GIF 动效等（jpg / jpeg / gif）
- **JSON 数据**：知识图谱（KG.json）、图谱结构（energy.json、flare.json）等可视化数据

## 使用

通过 raw 链接直接引用，或 clone 到本地使用。

## 维护

- 素材文件直接放在根目录，命名清晰、避免重复
- 已加入 `.gitignore`，避免 `.DS_Store` 等系统文件入库
